<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="10dp">

    <ImageView
        android:id="@+id/courseImageView"
        android:layout_width="match_parent"
        android:layout_height="100dp"
        android:scaleType="fitXY"
        android:src="@drawable/linkknown" />

    <TextView
        android:id="@+id/courseNameView"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/courseImageView"
        android:layout_alignLeft="@+id/courseImageView"
        android:layout_marginTop="5dp"
        android:ellipsize="end"
        android:maxLines="2"
        android:text="课程名称"
        android:textColor="@color/black"
        android:textSize="13sp" />

    <TextView
        android:id="@+id/courseTypeView"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/courseNameView"
        android:layout_alignLeft="@+id/courseImageView"
        android:layout_marginTop="2dp"
        android:ellipsize="end"
        android:text="课程类型"
        android:textSize="11sp" />
</RelativeLayout>
